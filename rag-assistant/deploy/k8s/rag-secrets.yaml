apiVersion: v1
kind: Secret
metadata:
  name: rag-secrets
  namespace: kubesphere-system
type: Opaque
data:
  # 使用前请替换为实际的base64编码值
  # 可以使用 echo -n "YOUR_API_KEY" | base64 生成
  llm-api-key: BASE64_ENCODED_LLM_API_KEY
  llm-api-url: BASE64_ENCODED_LLM_API_URL
