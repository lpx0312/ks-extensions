apiVersion: v1
kind: Service
metadata:
  name: rag-backend
  namespace: kubesphere-system
  labels:
    app: rag-backend
    component: rag-assistant
spec:
  selector:
    app: rag-backend
  ports:
  - name: http
    port: 8000
    targetPort: 8000
    protocol: TCP
  type: ClusterIP
