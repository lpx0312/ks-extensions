apiVersion: v1
kind: Service
metadata:
  name: rag-api
  namespace: kubesphere-system
  labels:
    app: rag-api
spec:
  selector:
    app: rag-api
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  type: ClusterIP
